#ifndef IMAGES_H
#define IMAGES_H

#include <stdio.h>
#include <stdlib.h>
#include <SDL/SDL.h>
#include <SDL/SDL_image.h>
#include <string.h>

typedef struct{
    SDL_Surface *surface;
    SDL_Rect position;
}Image;

static Image *menu_frame;

/*  ======================================================= 
        =======================================================
        ======================================================= 
        =======================================================
        ======================================================= 
        =======================================================
        ======================================================= 
        =======================================================
        ======================================================= 
        =======================================================         FUNCTIONS BELOW !!!
        ======================================================= 
        =======================================================
        ======================================================= 
        =======================================================
        ======================================================= 
        =======================================================
        ======================================================= 
            ======================================================= 
*/


/*
* get_extension() is used for asd
*/
short bmp_image(char* filename);

/*
* load_image() is used to load an image from a file.
*/
Image* load_image(char* filename);

#endif